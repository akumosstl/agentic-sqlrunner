{
  "name": "sql-runner",
  "version": "1.0.0",
  "description": "MCP server that connects to SQL databases (postgres, mysql, h2) and executes queries from natural language prompts via OpenCode",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mcp", "sql", "database", "postgres", "mysql", "h2", "opencode"],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "pg": "^8.13.1",
    "mysql2": "^3.12.0",
    "zod": "^3.25.76"
  }
}
